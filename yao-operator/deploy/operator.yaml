apiVersion: apps/v1
kind: Deployment
metadata:
  name: yao-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: yao-operator
  template:
    metadata:
      labels:
        name: yao-operator
    spec:
      serviceAccountName: yao-operator
      containers:
        - name: yao-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - yao-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "yao-operator"
